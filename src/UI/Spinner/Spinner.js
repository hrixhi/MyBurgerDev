import React from 'react';
import classes from './Spinner.css';

const Spinner = (props) => (
    <div style={{width: '100%', margin: 'auto'}}><div className={classes.Spinner}></div></div>
);

export default Spinner